# Benchmarks

This area holds reusable evaluation assets shared across experiments:

- versioned case sets and dataset manifests,
- benchmark protocols and metric definitions,
- baseline and candidate result summaries,
- confidence or uncertainty calculations,
- compatibility and environment records.

Do not tune against a protected holdout. Keep development, regression, and
holdout sets distinct and record their content hashes in result manifests.

